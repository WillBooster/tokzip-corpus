ata to reduce the load on the backend database. This article will briefly introduce some of the useful features of Redis and where it can be applied.

<!-- more -->

## Common Commands
[Redis's official website](http://redis.io/commands) lists every supported command. Let's start with the simplest:

### SET, GET

```
redis> SET mykey "Hello"
redis> GET mykey
"Hello"
```
As mentioned earlier, Redis is a key-value pair database. Therefore, the simplest SET is to set the value of a certain key, a